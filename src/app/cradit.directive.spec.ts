import { CraditDirective } from './cradit.directive';

describe('CraditDirective', () => {
  it('should create an instance', () => {
    const directive = new CraditDirective();
    expect(directive).toBeTruthy();
  });
});
